export enum OrderStatus {
  DRAFT = "draft",
  PENDING = "pending",
  CANCELLED = "cancelled",
  COMPLETED = "completed",
}

export enum PaymentMethod {
  CASH = "cash",
  BANK_TRANSFER = "bank_transfer",
  CREDIT_CARD = "credit_card",
}
